# Development profile.
#
# The security bootstrap still needs NEXTWARE_BOOTSTRAP_PASSWORD supplied via
# the environment (see backend/.env.example). Never hardcode a password here.

nextware.security.bootstrap.enabled=true
nextware.demo-data.enabled=true

# Log the SQL the seeder issues on first run.
spring.jpa.show-sql=false
